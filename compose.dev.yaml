services:
  postgres:
    ports:
      - "127.0.0.1:${POSTGRES_DEV_PORT:-5432}:5432"

  backend:
    environment:
      SPRING_PROFILES_ACTIVE: dev

  frontend:
    ports:
      - "127.0.0.1:${PUBLIC_HTTP_PORT:-8088}:8080"

networks:
  data:
    internal: false
  web:
    internal: false
